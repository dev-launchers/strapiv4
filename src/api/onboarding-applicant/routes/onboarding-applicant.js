'use strict';

/**
 * onboarding-applicant router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::onboarding-applicant.onboarding-applicant');
